# Тестирование онбординга салона 962302

**Дата:** 2025-12-04
**Салон:** KULTURA Малаховка (ID: 962302)
**WhatsApp номер салона:** 79686484488
**Тестовый номер клиента:** номер Арсена (LID: 152926689472618)

---

## Резюме

### Что работает:
- [x] Подключение через YClients маркетплейс
- [x] HMAC верификация подписи
- [x] Создание/обновление компании в БД
- [x] Генерация QR-кода на странице онбординга
- [x] Подключение WhatsApp через QR-код
- [x] Подключение WhatsApp через Pairing Code
- [x] Получение входящих сообщений
- [x] Обработка сообщений AI (Gemini)
- [x] Поиск слотов для записи в YClients
- [ ] **КРИТИЧЕСКИЙ БАГ:** Отправка ответов клиентам (400 ошибка)

### Найденные и исправленные баги:
1. **UTF-8 кодировка** - кракозябры вместо русских имён
2. **Отображение личных данных** - показывался личный телефон пользователя вместо данных салона
3. **JWT декодирование** - URL-safe base64 не декодировался правильно
4. **company_title** - название компании не передавалось в токен
5. **LID формат** - WhatsApp LID номера не поддерживались (частично исправлено)

### Оставшийся критический баг:
**Отправка сообщений через API возвращает 400 ошибку**

---

## Детальный отчёт

### 1. Подключение через YClients Маркетплейс

**Флоу:**
1. Пользователь нажимает "Подключить" в маркетплейсе YClients
2. YClients перенаправляет на `https://adminai.tech/auth/yclients/redirect?salon_id=962302&user_data=...&user_data_sign=...`
3. Сервер верифицирует HMAC подпись
4. Создаёт/обновляет компанию в БД
5. Генерирует JWT токен с данными компании
6. Перенаправляет на страницу онбординга

**Логи успешного подключения:**
```
✅ HMAC signature verified successfully
📋 Decoded and sanitized user_data: {salon_name: "KULTURA Малаховка", user_email: "borodach.malakhovka@yandex.ru", user_id: 6422675, user_name: "Арсен"}
✅ Компания создана/обновлена в БД: {company_id: 15, title: "KULTURA Малаховка", yclients_id: 962302}
🔄 Redirecting to onboarding
```

**Статус:** ✅ Работает

---

### 2. Страница онбординга

**URL:** `https://adminai.tech/marketplace/onboarding?token=<JWT>`

**Исправленные баги:**

#### 2.1 UTF-8 кодировка (commit c5e70ba)
**Проблема:** Имя "Арсен" отображалось как "ĐĒÑĒÑĒĐ¼Đ½"
**Причина:** `atob()` не поддерживает UTF-8
**Решение:** Добавлено правильное декодирование через `decodeURIComponent`

```javascript
// До:
tokenData = JSON.parse(atob(token.split('.')[1]));

// После:
const jsonPayload = decodeURIComponent(
    atob(base64Payload)
        .split('')
        .map(c => '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2))
        .join('')
);
```

#### 2.2 Отображение личных данных (commit 705c605)
**Проблема:** Показывался личный телефон пользователя YClients (79686484488)
**Причина:** Код использовал `user_data.user_phone` вместо данных салона
**Решение:** Убрано отображение личного телефона

#### 2.3 JWT URL-safe base64 (commit 3ec9f33)
**Проблема:** `tokenData` был `undefined`
**Причина:** JWT использует URL-safe base64 (`-` и `_` вместо `+` и `/`)
**Решение:** Добавлена конвертация перед декодированием

```javascript
base64Payload = base64Payload.replace(/-/g, '+').replace(/_/g, '/');
while (base64Payload.length % 4) {
    base64Payload += '=';
}
```

#### 2.4 company_title в JWT (commit 57cb7fa)
**Проблема:** Название компании не отображалось
**Причина:** `company_title` не добавлялся в JWT payload
**Решение:** Добавлен `company_title: company.title` в токен на бэкенде

**Файлы изменены:**
- `public/marketplace/onboarding.html`
- `src/api/routes/yclients-marketplace.js`

**Статус:** ✅ Работает (с исправлениями)

---

### 3. Подключение WhatsApp

**Два метода:**
1. QR-код (основной)
2. Pairing Code (альтернативный)

**Проблема с конфигурацией:**

В системе существует два формата `company_id`:
- `962302` - используется в `baileys-whatsapp-service`
- `company_962302` - используется в `ai-admin-api` при онбординге

Это приводит к дублированию credentials в БД:
```sql
SELECT company_id FROM whatsapp_auth;
-- 962302
-- company_962302
```

**Решение для тестирования:**
1. Обновлён `WHATSAPP_PHONE_NUMBER` в `.env` на 79686484488
2. Скопированы credentials из `company_962302` в `962302`
3. Перезапущен baileys-service с новым номером

**Pairing Code успешно сгенерирован:**
```
==============================================
PAIRING CODE: XAE8-1TV9
Phone: 79686484488
==============================================
```

**Статус подключения в БД:**
```sql
SELECT company_id, title, integration_status, whatsapp_connected, whatsapp_phone
FROM companies WHERE company_id = 962302;

-- company_id: 962302
-- title: KULTURA Малаховка
-- integration_status: active
-- whatsapp_connected: true
-- whatsapp_phone: 79686484488:30
```

**Health check baileys-service:**
```json
{
  "service": "baileys-whatsapp",
  "companyId": "962302",
  "status": "connected",
  "connected": true,
  "phoneNumber": "79686484488:31",
  "metrics": {
    "messagesSent": 18,
    "messagesReceived": 1,
    "connectedSessions": 1
  }
}
```

**Статус:** ✅ Работает

---

### 4. Получение и обработка сообщений

**Флоу:**
1. Клиент отправляет сообщение на WhatsApp салона
2. baileys-service получает сообщение
3. Сообщение попадает в очередь BullMQ
4. ai-admin-worker-v2 обрабатывает сообщение
5. AI (Gemini) анализирует и генерирует ответ
6. Worker отправляет ответ клиенту

**Логи успешной обработки:**
```
📩 Messages upsert for company company_962302, type: notify, count: 1
✅ AI Admin v2 completed in 9726ms
🤖 Bot sending 2 messages to 152926689472618 via whatsapp
```

**AI нашёл слоты для записи:**
```json
{
  "command": "SEARCH_SLOTS",
  "data": [
    {"datetime": "2025-12-04T20:30:00+03:00", "staff_name": "Бари", "time": "20:30"},
    {"datetime": "2025-12-04T21:00:00+03:00", "staff_name": "Бари", "time": "21:00"}
  ],
  "service": "МУЖСКАЯ СТРИЖКА",
  "success": true
}
```

**Статус:** ✅ Работает (получение и обработка)

---

### 5. КРИТИЧЕСКИЙ БАГ: Отправка ответов

**Проблема:**
Ответы бота не доставляются клиентам. Ошибка 400 при отправке.

**Причина:**
WhatsApp использует LID (внутренние ID) для некоторых контактов. Входящий номер клиента - `152926689472618` (LID), а не обычный телефонный номер.

При отправке:
- Worker вызывает `whatsappClient.sendMessage('152926689472618', message)`
- `client.js` форматирует номер как `152926689472618@c.us`
- Baileys не может найти такой контакт и возвращает 400
- Правильный формат: `152926689472618@lid`

**Тест напрямую через baileys-service:**
```bash
# Неправильный формат (400 ошибка):
curl -X POST http://localhost:3003/send \
  -d '{"phone":"152926689472618@c.us","message":"Test"}'
# => 400 error

# Правильный формат (успех):
curl -X POST http://localhost:3003/send \
  -d '{"phone":"152926689472618@lid","message":"Test"}'
# => {"success":true,"messageId":"..."}
```

**Частичное исправление (commit 5331a59):**
Добавлена логика распознавания LID в `_formatPhone()`:

```javascript
// Если номер >= 15 цифр, это вероятно LID
if (cleanPhone.length >= 15) {
  return `${cleanPhone}@lid`;
}
```

**Статус:** ❌ НЕ РАБОТАЕТ

Фикс был задеплоен, но проблема сохраняется. Требуется дальнейшая диагностика:
1. Возможно код не обновился на сервере
2. Возможно форматирование происходит в другом месте
3. Возможно проблема в middleware между worker и baileys-service

---

## Коммиты сессии

| Commit | Описание |
|--------|----------|
| c5e70ba | fix: UTF-8 decoding for Russian text in onboarding page |
| 705c605 | fix: show salon name instead of user's personal data on onboarding page |
| 57cb7fa | fix: display company title on onboarding page |
| 3ec9f33 | fix: JWT URL-safe base64 decoding on onboarding page |
| 5331a59 | fix: support LID phone format for WhatsApp message sending |

---

## Следующие шаги

### Срочно (блокер):
1. **Исправить отправку сообщений с LID форматом**
   - Проверить, что фикс применился на сервере
   - Добавить логирование форматирования номера
   - Протестировать разные форматы номеров

### Улучшения онбординга:
2. **Унифицировать формат company_id**
   - Решить: `962302` или `company_962302`
   - Обновить все компоненты для единого формата

3. **Улучшить страницу онбординга**
   - Автоматический редирект после подключения WhatsApp
   - Показывать статус подключения в реальном времени
   - Добавить обработку ошибок

4. **WebSocket не работает для уведомлений**
   - Событие `whatsapp-connected` не отправляется на фронтенд
   - Страница не переходит на шаг 2 автоматически

---

## Конфигурация для тестирования

### .env на сервере (релевантные переменные):
```bash
WHATSAPP_PHONE_NUMBER=79686484488
BAILEYS_SERVICE_URL=http://localhost:3003
YCLIENTS_COMPANY_ID=962302
```

### База данных:
```sql
-- Проверка статуса компании
SELECT * FROM companies WHERE company_id = 962302;

-- Проверка credentials
SELECT company_id, created_at FROM whatsapp_auth
WHERE company_id IN ('962302', 'company_962302');
```

### PM2 сервисы:
```bash
pm2 status
# ai-admin-api - online
# ai-admin-worker-v2 - online
# baileys-whatsapp-service - online
```

---

## Файлы изменённые в этой сессии

1. `public/marketplace/onboarding.html` - 4 фикса
2. `src/api/routes/yclients-marketplace.js` - добавлен company_title в JWT
3. `src/integrations/whatsapp/client.js` - поддержка LID формата

---

**Автор:** Claude Code
**Время тестирования:** ~2 часа
