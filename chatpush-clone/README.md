# ChatPush Clone - Платформа многоканальных рассылок

Современный сайт с возможностью выбора тарифных планов и онлайн оплаты без перехода на сторонние сайты.

## 🚀 Возможности

- ✅ Современный адаптивный дизайн
- ✅ Выбор тарифных планов
- ✅ Модальное окно оплаты
- ✅ Интеграция с платежными системами:
  - YooKassa (ЮKassa) - основная для России
  - Stripe - международные платежи
  - Tinkoff - альтернатива для России
- ✅ Поддержка оплаты картой
- ✅ Выставление счетов для организаций
- ✅ Валидация данных карты
- ✅ Безопасная обработка платежей

## 📁 Структура проекта

```
chatpush-clone/
├── index.html           # Главная страница
├── css/
│   └── styles.css      # Стили сайта
├── js/
│   ├── main.js         # Основная логика
│   └── payment-integration.js  # Интеграция с платежами
├── api/
│   └── server.js       # Серверная часть (Node.js)
├── assets/             # Изображения и иконки
├── package.json        # Зависимости проекта
└── .env.example        # Пример конфигурации
```

## 🛠 Установка и запуск

### 1. Клонируйте проект
```bash
cd chatpush-clone
```

### 2. Установите зависимости
```bash
npm install
```

### 3. Настройте переменные окружения
```bash
cp .env.example .env
```

Отредактируйте файл `.env` и добавьте ваши ключи:
- `YOOKASSA_SHOP_ID` - ID магазина в ЮKassa
- `YOOKASSA_SECRET_KEY` - Секретный ключ ЮKassa
- Другие ключи платежных систем при необходимости

### 4. Запустите сервер
```bash
# Production
npm start

# Development (с автоперезагрузкой)
npm run dev
```

### 5. Откройте сайт
```bash
# В отдельном терминале запустите локальный сервер для фронтенда
npm run client
```

Сайт будет доступен по адресу: http://localhost:8080

## 💳 Настройка платежных систем

### YooKassa (ЮKassa)

1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. Создайте магазин
3. Получите Shop ID и Secret Key
4. Добавьте webhook URL: `http://ваш-домен/api/payment/webhook`
5. Настройте HTTP-уведомления в личном кабинете

### Stripe

1. Зарегистрируйтесь на [stripe.com](https://stripe.com)
2. Получите API ключи в Dashboard
3. Настройте webhook endpoints
4. Добавьте ключи в `.env`

### Tinkoff

1. Подключите интернет-эквайринг на [business.tinkoff.ru](https://business.tinkoff.ru)
2. Получите Terminal Key и Secret Key
3. Настройте URL для уведомлений
4. Добавьте ключи в `.env`

## 🔧 API Endpoints

### POST `/api/payment/create`
Создание платежа

Параметры:
```json
{
  "email": "user@example.com",
  "name": "Иван Иванов",
  "phone": "+79991234567",
  "plan": "business",
  "paymentMethod": "card"
}
```

### POST `/api/payment/webhook`
Webhook для обработки уведомлений от платежной системы

### GET `/api/payment/status/:paymentId`
Проверка статуса платежа

### POST `/api/invoice/create`
Создание счета для юридических лиц

## 📱 Адаптивность

Сайт полностью адаптирован для всех устройств:
- 📱 Мобильные телефоны (320px+)
- 📱 Планшеты (768px+)
- 💻 Ноутбуки (1024px+)
- 🖥 Десктопы (1920px+)

## 🔐 Безопасность

- ✅ Валидация всех входящих данных
- ✅ HTTPS для всех платежных операций
- ✅ Соответствие PCI DSS стандартам
- ✅ Токенизация данных карт
- ✅ Защита от XSS и CSRF атак

## 🚀 Развертывание

### Heroku
```bash
heroku create your-app-name
heroku config:set YOOKASSA_SHOP_ID=your_shop_id
heroku config:set YOOKASSA_SECRET_KEY=your_secret_key
git push heroku main
```

### Docker
```bash
docker build -t chatpush-clone .
docker run -p 3001:3001 --env-file .env chatpush-clone
```

### PM2
```bash
pm2 start api/server.js --name chatpush-api
pm2 save
pm2 startup
```

## 📞 Поддержка

Если у вас возникли вопросы, создайте issue в репозитории.

## 📄 Лицензия

MIT License