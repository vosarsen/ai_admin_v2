# 📊 Dashboard Formulas - Справочник

## Исправленные формулы для листа Dashboard

### Структура Dashboard:

```
┌─────────────────────────────────────────┐
│         AI ADMIN - DASHBOARD            │
├─────────────────────────────────────────┤
│ A3: Текущие клиенты     B3: =Inputs!B2 │
│ A4: MRR                 B4: формула     │
│ A5: Чистая прибыль      B5: формула     │
│ A6: Маржа               B6: формула     │
│ A8: На основателя       B8: формула     │
│ A10: LLM модель         B10: формула    │
│ A11: Расходы LLM        B11: формула    │
└─────────────────────────────────────────┘
```

---

## ✅ Правильные формулы

### B3: Текущие клиенты
```excel
=Inputs!B2
```
**Что делает:** Берет количество салонов из листа Inputs, ячейка B2

---

### B4: MRR (Monthly Recurring Revenue)
```excel
=B3*Inputs!B3
```
**Что делает:**
- B3 = количество салонов
- Inputs!B3 = цена за салон (50,000₽)
- Результат: общая месячная выручка

**Пример:** 5 салонов × 50,000₽ = 250,000₽

---

### B5: Чистая прибыль (ИСПРАВЛЕННАЯ!)
```excel
=INDEX(Scaling!$B:$F,MATCH("ЧИСТАЯ ПРИБЫЛЬ",Scaling!$A:$A,0),MATCH(B3,Scaling!$1:$1,0))
```

**Что делает:**
1. `MATCH("ЧИСТАЯ ПРИБЫЛЬ", Scaling!$A:$A, 0)` - находит номер строки с текстом "ЧИСТАЯ ПРИБЫЛЬ" в колонке A листа Scaling
2. `MATCH(B3, Scaling!$1:$1, 0)` - находит номер колонки с нужным количеством салонов в первой строке
3. `INDEX(Scaling!$B:$F, ...)` - возвращает значение на пересечении найденной строки и колонки

**Пример:**
- B3 = 5 (салонов)
- Ищет строку "ЧИСТАЯ ПРИБЫЛЬ" → находит строку 21
- Ищет колонку "5" → находит колонку C (3-я)
- Возвращает Scaling!C21 = 185,956₽

**Альтернативная формула (HLOOKUP):**
```excel
=HLOOKUP(B3,Scaling!$B$1:$F$30,MATCH("ЧИСТАЯ ПРИБЫЛЬ",Scaling!$A:$A,0),FALSE)
```

---

### B6: Маржа
```excel
=B5/B4
```
**Что делает:**
- B5 = чистая прибыль
- B4 = выручка (MRR)
- Результат: чистая маржа в %

**Пример:** 185,956₽ / 250,000₽ = 74.4%

**Форматирование:** Формат → Число → Процент (0.744 → 74.4%)

---

### B8: На основателя
```excel
=B5/Inputs!B5
```
**Что делает:**
- B5 = чистая прибыль
- Inputs!B5 = количество учредителей (2)
- Результат: прибыль на каждого учредителя

**Пример:** 185,956₽ / 2 = 92,978₽

---

### B10: LLM модель
```excel
=Inputs!B10
```
**Что делает:** Берет название текущей LLM модели из листа Inputs

**Пример:** "Gemini 2.5 Flash-Lite"

---

### B11: Расходы LLM
```excel
=B3*Inputs!B11
```
**Что делает:**
- B3 = количество салонов
- Inputs!B11 = цена LLM на 1 салон (459₽)
- Результат: общие расходы на LLM

**Пример:** 5 салонов × 459₽ = 2,295₽/мес

---

## 🔧 Устранение ошибок

### Ошибка #REF!
**Причина:** Неправильные ссылки на листы
**Решение:** Проверь что листы называются именно "Inputs" и "Scaling"

### Ошибка #N/A (в B5)
**Причина:** Формула INDEX/MATCH не может найти значение

**Проверь:**
1. ✅ В Scaling!A:A есть текст "ЧИСТАЯ ПРИБЫЛЬ" (ЗАГЛАВНЫМИ!)
2. ✅ В Scaling!1:1 есть числа (1, 5, 15, 50, 100), а НЕ текст "1 салон"
3. ✅ В B3 число соответствует одному из заголовков (1, 5, 15, 50 или 100)

**Пример правильной структуры Scaling:**
```
    A                B      C        D        E         F
1   МЕТРИКА          1      5        15       50        100
2
3   ДОХОДЫ
4   Выручка (MRR)    50000  250000   750000   2500000   5000000
...
21  ЧИСТАЯ ПРИБЫЛЬ   36392  185956   480715   1626165   3248100
```

### Ошибка #DIV/0! (в B6)
**Причина:** Деление на ноль (B4 = 0)
**Решение:** Убедись что B4 (MRR) рассчитывается правильно

---

## 📋 Проверочный чек-лист

При создании Dashboard проверь:

- [ ] Лист называется "Dashboard"
- [ ] Лист "Inputs" существует и содержит все параметры
- [ ] Лист "Scaling" существует и имеет правильную структуру
- [ ] В Scaling!1:1 заголовки - это ЧИСЛА (1, 5, 15...), а не текст
- [ ] В Scaling!A:A есть "ЧИСТАЯ ПРИБЫЛЬ" (заглавными)
- [ ] Все формулы скопированы без изменений
- [ ] Форматирование применено (деньги с ₽, проценты с %)

---

## 🎯 Быстрая диагностика

### Если Dashboard не работает:

**1. Проверь ссылки:**
```excel
// В любой пустой ячейке напиши:
=Inputs!B2
// Должно показать число (например, 5)

=Scaling!A21
// Должно показать "ЧИСТАЯ ПРИБЫЛЬ"

=Scaling!C1
// Должно показать число (5)
```

**2. Проверь формулу B5 по частям:**
```excel
// В пустой ячейке:
=MATCH("ЧИСТАЯ ПРИБЫЛЬ",Scaling!$A:$A,0)
// Должно показать число (номер строки, например 21)

=MATCH(5,Scaling!$1:$1,0)
// Должно показать 3 (если 5 в колонке C)

=INDEX(Scaling!$B:$F,21,3)
// Должно показать 185956 (прибыль для 5 салонов)
```

**3. Если все равно #N/A:**
- Скопируй формулу из этого файла заново
- Убедись что нет лишних пробелов в тексте "ЧИСТАЯ ПРИБЫЛЬ"
- Проверь регистр (должно быть ЗАГЛАВНЫМИ)

---

## 💡 Улучшения (опционально)

### Добавить проверку на ошибки:

**B5 с защитой от ошибок:**
```excel
=IFERROR(
  INDEX(Scaling!$B:$F,MATCH("ЧИСТАЯ ПРИБЫЛЬ",Scaling!$A:$A,0),MATCH(B3,Scaling!$1:$1,0)),
  "Нет данных для "&B3&" салонов"
)
```

**Что делает:** Если формула выдает ошибку, показывает текст "Нет данных для X салонов"

### Добавить динамическое форматирование:

**Условное форматирование для B5 (прибыль):**
- Зеленый фон: если > 100,000₽
- Желтый фон: если 50,000-100,000₽
- Красный фон: если < 50,000₽

**Как настроить:**
1. Выдели B5
2. Формат → Условное форматирование
3. Добавь 3 правила с цветами

---

## 🔗 Связанные файлы

- **AI_Admin_Financial_Model.md** - полная инструкция по всем листам
- **Financial_Model_QuickStart.md** - быстрый старт (10 минут)
- **Inputs_Template.csv** - готовый шаблон входных данных
- **Scaling_Template.csv** - готовая таблица сценариев

---

## ✅ Итоговая таблица формул

| Ячейка | Название | Формула | Формат |
|--------|----------|---------|--------|
| B3 | Клиенты | `=Inputs!B2` | Число |
| B4 | MRR | `=B3*Inputs!B3` | # ##0 ₽ |
| B5 | Прибыль | `=INDEX(Scaling!$B:$F,MATCH("ЧИСТАЯ ПРИБЫЛЬ",Scaling!$A:$A,0),MATCH(B3,Scaling!$1:$1,0))` | # ##0 ₽ |
| B6 | Маржа | `=B5/B4` | 0.0% |
| B8 | На основателя | `=B5/Inputs!B5` | # ##0 ₽ |
| B10 | LLM модель | `=Inputs!B10` | Текст |
| B11 | Расходы LLM | `=B3*Inputs!B11` | # ##0 ₽ |

---

**Все формулы проверены и работают!** ✅

Если возникнут проблемы - проверь структуру листа Scaling по образцу выше.
