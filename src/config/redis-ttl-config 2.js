/**
 * Конфигурация TTL для Redis ключей
 * Все значения в секундах
 */

module.exports = {
  // Долгосрочные предпочтения клиента (90 дней)
  preferences: 90 * 24 * 60 * 60,
  
  // Активный диалог (24 часа)
  dialog: 24 * 60 * 60,
  
  // Кэш клиента из базы данных (7 дней)
  client: 7 * 24 * 60 * 60,
  
  // История сообщений (30 дней)
  messages: 30 * 24 * 60 * 60,
  
  // Информация о букинге (365 дней)
  booking: 365 * 24 * 60 * 60,
  
  // Контекст напоминания (24 часа)
  reminder_context: 24 * 60 * 60,
  
  // Полный контекст (кэш) (5 минут)
  fullContext: 5 * 60,
  
  // Статус обработки (1 час)
  processing: 60 * 60,
  
  // Rate limiting - день (24 часа)
  rateDay: 24 * 60 * 60,
  
  // Rate limiting - час (1 час)
  rateHour: 60 * 60,
  
  // Rate limiting - минута (1 минута)
  rateMinute: 60
};